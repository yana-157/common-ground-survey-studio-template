# Tileset configurations

Upload reviewed Census/Mapbox configuration JSON files to this folder through GitHub’s **Add file → Upload files** page. Keep Census and Mapbox credentials in the repository’s Actions secrets, not in these files.
